import Image from 'next/image'
import type { CmsTeamMember } from '../lib/cms'

export default function Team({ members }: { members: CmsTeamMember[] }) {
  if (!members.length) return null
  return <section className="relative overflow-hidden px-4 py-28 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><div className="text-center"><p className="text-xs font-bold uppercase tracking-[.22em] text-[#2CCDDE]">Our Team</p><h2 className="mt-5 text-4xl font-bold sm:text-5xl">The people behind Byteflow</h2><p className="mx-auto mt-4 max-w-2xl text-[var(--text-muted)]">Experienced specialists working together across IT, cloud, security, design and growth.</p></div><div className="mt-14 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{members.map((member)=><article key={member.id} className="overflow-hidden rounded-2xl border border-cyan-400/15 bg-[var(--bg-surface)]"><div className="relative aspect-[4/3] bg-cyan-400/5">{member.image_url && <Image src={member.image_url} alt={member.image_alt || member.name} fill className="object-cover" />}</div><div className="p-6"><h3 className="text-xl font-bold">{member.name}</h3><p className="mt-1 text-sm font-semibold text-[#2CCDDE]">{member.job_title}</p>{member.biography && <p className="mt-4 text-sm leading-relaxed text-[var(--text-muted)]">{member.biography}</p>}<div className="mt-4 flex gap-3 text-xs text-[var(--text-muted)]">{member.email && <a href={`mailto:${member.email}`}>Email</a>}{member.linkedin_url && <a href={member.linkedin_url} target="_blank" rel="noopener noreferrer">LinkedIn</a>}{member.github_url && <a href={member.github_url} target="_blank" rel="noopener noreferrer">GitHub</a>}</div></div></article>)}</div></div></section>
}
